cd 
mkdir deploy
cd deploy
mkdir blue.git
mkdir blue
cd blue.git
git init --bare
cd 
echo 'GIT_WORK_TREE=/deploy/blue/ git checkout -f' > /deploy/blue.git/hooks/post-receive
chmod +x /deploy/blue.git/hooks/post-receive
